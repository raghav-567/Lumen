# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.2
psycopg2-binary==2.9.9

# Redis & Celery
redis==5.1.1
celery[redis]==5.4.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# Rate limiting
limits==5.8.0

# AI / ML
sentence-transformers==3.1.0
chromadb==0.5.7
# Pin posthog to the 2.x line: chromadb 0.5.7 calls posthog.capture() with three
# positional args, which posthog>=3 rejects ("capture() takes 1 positional
# argument but 3 were given") — the telemetry spam in the logs. 2.x keeps the
# old signature so the (disabled) telemetry path stops erroring.
posthog==2.5.0
google-genai==1.1.0
networkx==3.3
rank-bm25==0.2.2
scikit-learn>=1.4.0

# Document Parsing
PyMuPDF==1.24.10
python-docx==1.1.2

# Utils
pydantic==2.9.2
email-validator==2.2.0
pydantic-settings==2.5.2
python-dateutil==2.9.0
httpx==0.27.2
requests==2.32.3
numpy>=1.26.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
